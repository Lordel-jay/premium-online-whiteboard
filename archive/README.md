# Archive

This folder holds inactive or partially moved project work that is not part of the current app runtime.

- `admin-prototype/` is an older admin workspace.
- `frontend-legacy/` holds older frontend material that should not be used for current development.

These folders are being kept for reference until they can be removed safely from a Windows/OneDrive environment without permission errors.
